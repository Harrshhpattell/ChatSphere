import { Button } from "@/components/ui/button";
import { Link } from "react-router";

const Chat = () => {
  return (
    <div>
      <Link to="/">
        <Button>Okayyyy</Button>
      </Link>
    </div>
  );
};

export default Chat;
